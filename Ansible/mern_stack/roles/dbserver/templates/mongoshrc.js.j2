const url ="managedb://{{ mongo_root_user }}:{{ mongo_root_password }}@localhost:{{ mongodb_port }}/admin";
const dbName = "admin";
const connection = new Mongo(url);
const db = connection.getDB(dbName);